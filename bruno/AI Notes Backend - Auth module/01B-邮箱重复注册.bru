meta {
  name: 01B-邮箱重复注册
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/auth/register
  body: json
  auth: none
}

body:json {
  {
    "email": "{{testEmail}}",
    "password": "anotherPassword123"
  }
}

assert {
  res.status: eq 400
  res.body.error: isDefined
}
