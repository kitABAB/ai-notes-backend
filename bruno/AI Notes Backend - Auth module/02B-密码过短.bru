meta {
  name: 02B-密码过短
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "{{testEmail}}",
    "password": "123"
  }
}

assert {
  res.status: eq 400
}
